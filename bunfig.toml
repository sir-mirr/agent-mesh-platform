[test]
# Runs before any test file, which is the only place this can be decided: the
# database paths are computed when their module loads, and in a single-process
# run the first import wins. See scripts/test-state-dir.ts.
preload = ["./scripts/test-state-dir.ts"]
