# Environment Configuration Template
#
# Empty is the working value for both the local proxy and the documented
# deployment: the front end calls relative paths and the browser resolves them
# against the page's origin. Fill this in only for the absolute-URL branch, and
# then set `AGENT_MESH_ALLOWED_ORIGINS` on the http server to match.
VITE_API_BASE_URL=
