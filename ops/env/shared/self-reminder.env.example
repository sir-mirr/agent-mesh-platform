SELF_REMINDER_IDENTITY=self-reminder-lab
SELF_REMINDER_POLL_MS=1000
# Comma-separated mesh identities notified when the scheduler recovers from a
# hub outage. Deployment-specific; unset means the recovery event is recorded
# in scheduler_events but no alert is routed.
SELF_REMINDER_RECOVERY_ALERT_RECIPIENTS=
